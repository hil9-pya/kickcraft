<IfModule mod_rewrite.c>
    RewriteEngine On
</IfModule>

<FilesMatch "^\.env(?:\.|$)">
    Require all denied
</FilesMatch>

<FilesMatch "^(?:\.git|composer\.(?:json|lock))">
    Require all denied
</FilesMatch>
